name: Build and Push Docker Image

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:  # 保留手动触发功能

env:
  IMAGE_NAME: redink  # 仅保留镜像名，移除多余的DOCKER_USERNAME

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    # GHCR推送必需：授予Packages写入权限和代码读取权限
    permissions:
      packages: write
      contents: read

    steps:
      - name: Checkout 代码
        uses: actions/checkout@v4

      - name: 设置 Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 登录GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io  # 固定GHCR仓库地址
          username: ${{ github.actor }}  # GitHub自动获取用户名，无需手动配置
          password: ${{ secrets.GITHUB_TOKEN }}  # 自动提供的令牌，无需手动添加Secrets

      - name: 提取 Docker 元数据（适配GHCR格式）
        id: meta
        uses: docker/metadata-action@v5
        with:
          # 关键修正：GHCR镜像格式固定为 ghcr.io/[GitHub用户名]/[镜像名]
          images: ghcr.io/${{ github.actor }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
            type=raw,value=latest,enable={{is_default_branch}}

      - name: 构建并推送 Docker 镜像
        uses: docker/build-push-action@v5
        with:
          context: .
          platforms: linux/amd64,linux/arm64  # 保留多架构构建（可选）
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: 输出镜像信息
        run: |
          echo "🎉 Docker 镜像构建成功！"
          echo "镜像标签："
          echo "${{ steps.meta.outputs.tags }}"
